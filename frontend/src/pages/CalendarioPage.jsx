import {useEffect,useState} from 'react';import {agendaService,proyectosService} from '../services/api.js';
export default function CalendarioPage(){const [projects,setProjects]=useState([]);const [pid,setPid]=useState('');const [events,setEvents]=useState([]);useEffect(()=>{proyectosService.getAll().then(r=>setProjects(r.data.data||[]));},[]);useEffect(()=>{if(pid)agendaService.calendar(pid).then(r=>setEvents(r.data.data||[]));},[pid]);return <div className="page"><div className="page-header"><div><h1>Calendario de actividades</h1><p>Fechas de tareas, entregables y reuniones.</p></div><select className="form-select" style={{maxWidth:320}} value={pid} onChange={e=>setPid(e.target.value)}><option value="">Selecciona un proyecto</option>{projects.map(p=><option key={p.id_proyecto} value={p.id_proyecto}>{p.nombre}</option>)}</select></div><div className="card card-body">{!pid?<p>Selecciona un proyecto.</p>:events.length===0?<p>No hay actividades registradas.</p>:<div className="table-wrap"><table><thead><tr><th>Tipo</th><th>Actividad</th><th>Inicio</th><th>Fin</th></tr></thead><tbody>{events.sort((a,b)=>String(a.start).localeCompare(String(b.start))).map(e=><tr key={`${e.type}-${e.id}`}><td><span className="badge badge-blue">{e.type}</span></td><td>{e.title}</td><td>{String(e.start).slice(0,16).replace('T',' ')}</td><td>{String(e.end).slice(0,16).replace('T',' ')}</td></tr>)}</tbody></table></div>}</div></div>}
