# Frontend — SGP SENA

Bases HTML/CSS/JS por rol, migradas desde el borrador original (`Docs/HTML_BASES`).

```
frontend/
├── admin/
├── aprendiz/
└── instructor/
```

## Estado actual
Estas son plantillas base (borrador), no un proyecto frontend con build tool todavía.

## Pendiente por definir
- [ ] Decidir si se migra a un framework (React/Vue/Angular) o se mantiene HTML/CSS/JS plano
- [ ] Conectar con la API definida en `../docs/03-api/api-rest.md`
- [ ] Definir carpeta de assets compartidos (logos, íconos) si aplica
