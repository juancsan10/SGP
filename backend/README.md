# Backend — SGP SENA

Aquí vive el código del servidor (API) del sistema.

## Pendiente por definir
- [ ] Elegir stack (Node/Express, Java/Spring, PHP/Laravel, etc.)
- [ ] Conexión a la base de datos, que corre en Docker (ver `../docker-compose.yml` y `../database/`)
- [ ] Variables de entorno: usar `../.env` (basado en `../.env.example`) para host, puerto, usuario y clave de la BD

## Referencia
La especificación de endpoints está documentada en `../docs/03-api/api-rest.md`.
