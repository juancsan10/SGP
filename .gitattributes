# Saltos de linea LF en todo el repo (evita CRLF dentro de las imagenes Docker
# y los "M" falsos entre Windows y Linux).
* text=auto eol=lf

# Binarios: nunca tocar
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.m4a  binary
*.drawio -text
