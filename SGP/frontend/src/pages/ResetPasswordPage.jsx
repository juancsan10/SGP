import {useState} from 'react';import {useNavigate,useSearchParams} from 'react-router-dom';import {passwordService} from '../services/api.js';
export default function ResetPasswordPage(){const [params]=useSearchParams();const nav=useNavigate();const [pwd,setPwd]=useState('');const [msg,setMsg]=useState('');const submit=async e=>{e.preventDefault();try{const r=await passwordService.confirm(params.get('token'),pwd);setMsg(r.data.message);setTimeout(()=>nav('/login'),1200);}catch(err){setMsg(err.response?.data?.message||'Token inválido o expirado');}};return <div style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'var(--slate-100)'}}><form className="card card-body" onSubmit={submit} style={{width:'min(420px,92vw)'}}><h2>Restablecer contraseña</h2><p style={{margin:'8px 0 16px',color:'var(--slate-500)'}}>Elige una nueva contraseña de mínimo 8 caracteres.</p><input className="form-input" type="password" minLength="8" value={pwd} onChange={e=>setPwd(e.target.value)} required placeholder="Nueva contraseña"/><button className="btn btn-primary" style={{marginTop:12}}>Actualizar contraseña</button>{msg&&<p style={{marginTop:12}}>{msg}</p>}</form></div>}
