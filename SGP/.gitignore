# Entorno / credenciales
.env

# Node / frontend
node_modules/
dist/
build/

# Este proyecto usa pnpm (ver "packageManager" en package.json) — si por error
# se corre "npm install", NO versionar el lockfile de npm que genera.
package-lock.json
yarn.lock

# Backend (Node.js)
*.log

# Sistema
.DS_Store
Thumbs.db

# Backups locales de base de datos
database/backups/*
!database/backups/.gitkeep
